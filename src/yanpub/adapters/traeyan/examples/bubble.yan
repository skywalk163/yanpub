---
title: '冒泡排序'
tags: ['排序', '算法']
difficulty: '中等'
description: '经典排序算法'
---

# 趣言 - 列表操作演示
# 演示列表创建和访问

印"=== 列表演示 ==="。

定nums等于列(5,3,8,1,2)。
印"列表内容："。
印nums。

印"第一个元素："。
印取(nums,0)。

印"第二个元素："。
印取(nums,1)。

印"列表长度："。
印长度(nums)。