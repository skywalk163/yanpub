---
title: '高级功能'
tags: ['进阶', '列表', '函数']
difficulty: '进阶'
description: '列表操作、斐波那契、高阶函数与条件嵌套'
---

# 光明示例程序 - 高级功能

# 1. 列表操作
设 列表 为 [1, 2, 3, 4, 5]。
打印("列表：")。
打印(列表)。

# 2. 斐波那契数列
段落 斐波那契 接收 数：
  如果数小于等于二那么返回一。
  返回斐波那契(数减一)加斐波那契(数减二)。
结束。

打印("斐波那契数列：")。
设 计数 为 一。
当 计数 小于等于 十：
  打印(斐波那契(计数))。
  设 计数 为 计数 加 一。
结束。

# 3. 函数参数（高阶函数思想）
段落 计算器 接收 操作符, 甲, 乙：
  如果操作符等于"加"那么返回甲加乙。
  如果操作符等于"乘"那么返回甲乘乙。
  返回 0。
结束。

打印("函数参数：")。
打印(计算器("加", 三, 五))。
打印(计算器("乘", 三, 五))。

# 4. 条件嵌套
段落 评级 接收 分数：
  如果分数大于等于九十那么返回"优秀"。
  如果分数大于等于八十那么返回"良好"。
  如果分数大于等于七十那么返回"中等"。
  如果分数大于等于六十那么返回"及格"。
  返回"不及格"。
结束。

打印("成绩评级：")。
打印(评级(八五))。

打印("高级功能演示完成！")。