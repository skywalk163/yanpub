---
title: '计算器'
tags: ['面向对象', '类']
difficulty: '基础'
description: '用类与方法实现一个可视化计算器'
---

# 光明编程语言 - 计算器示例
# 演示类定义、属性和方法

类 计算器：
  属性 结果 等于 0。

  段落 加数 接收 x：
    己结果 等于 己结果 加 x。

  段落 减数 接收 x：
    己结果 等于 己结果 减 x。

  段落 乘数 接收 x：
    己结果 等于 己结果 乘 x。

  段落 除数 接收 x：
    如果 x 不等于 0：
      己结果 等于 己结果 除 x。
    否则：
      打印("错误: 除数不能为零")

  段落 清零：
    己结果 等于 0。

  段落 获取结果：
    返回 己结果。

# 使用计算器
计算器1 等于 新建 计算器()。
计算器1.加数(10)。
计算器1.乘数(2)。
计算器1.减数(5)。
打印("计算结果: " + 转字符串(计算器1.获取结果()))。