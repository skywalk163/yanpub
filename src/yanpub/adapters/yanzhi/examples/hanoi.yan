---
title: '阶乘与管道'
tags: ['递归', '管道']
difficulty: '中等'
description: '阶乘递归和管道式编程'
---

# 言知 - 阶乘与管道

# 递归阶乘
定义 fac = 函数 n：
  如果 n 等于 1 那么 返回 1。
  定义 t = 相减 n 1。
  定义 r = fac t。
  相乘 n r。
结束。

定义 r1 = fac 5。
打印 r1。
定义 r2 = fac 8。
打印 r2。

# 管道操作
定义 data = 列表 1 2 3 4 5。
定义 mapped = data，映射 相乘 2。
打印 mapped。
定义 chained = data，映射 相乘 2，过滤 大于 5，归约 相加 0。
打印 chained。
