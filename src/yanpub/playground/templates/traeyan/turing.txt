# 趣言 - 状态机演示
# 演示状态切换和循环

印"=== 状态机演示 ==="。

定state等于"开始"。
定count等于0。

当state不等"结束"：
  印"当前状态："加state。
  
  若state等"开始"：
    印"正在初始化..."。
    定state等于"运行"。
  结束
  
  若state等"运行"：
    印"正在运行..."。
    定count等于加count 1。
    若count大3：
      定state等于"结束"。
    结束
  结束
结束。

印"状态机结束"。
印"运行次数："加count。